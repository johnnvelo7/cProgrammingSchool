#include <stdio.h>
#include <stdlib.h>

int main() {
    int size = 5;
    int *ptr = malloc(sizeof(int));
  
    
    printf("malin er best ingen protest. :DDDD\n");
    
    
    /*
    
    for (int i = 0; i < size; i++) {
        ptr[i] = i;
    }
    
    for (int i = 0; i < size; i++) {
        printf("Element %d: %d\n", i, ptr[i]);
    } */
    

    return 0;
}

